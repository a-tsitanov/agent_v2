# Backlog

Открытые вопросы, отложенные сознательно. Ничего из этого сейчас не ломается —
это выбор «не сейчас», а не забытые дефекты.

Дата: 2026-08-16. Сопутствующие документы: [`ROADMAP-contours.md`](ROADMAP-contours.md),
[`ROADMAP.md`](ROADMAP.md).

---

## Требует решения о методике, не о коде

### Канал оценки не подаётся в сравнение

`polarity_evolution` отдаёт **разбивку**, а не ряд: на каждый период по строке на
каждую полярность (`{period, polarity, n}`) — «май: негатив 120, нейтрально 30,
позитив 40». `stat_align` сравнивает два ряда, по одному числу с каждой стороны
на период.

Свернуть разбивку в одно число можно по-разному, и варианты не эквивалентны:
только негатив; негатив минус позитив; доля негатива от всех упоминаний. Выбор
меняет и результат, и смысл.

Пока не решено, сравнение каналов с опросами работает **только по вниманию**
(`topic_trend`). Спека называет `polarity_evolution` вторым канальным входом —
этот пункт закрыт документацией, а не кодом.

Ещё одно ограничение того же места: метки периодов у него всегда месячные
(`substr(valid_from,0,7)` в обеих реализациях graph-ops), то есть недельное
сравнение — под которое `stat_align` и настроен по умолчанию — им не покрывается.

---

## Очередь работ

1. **Пробелы техдолга** — `docs/superpowers/plans/2026-08-16-close-stats-gaps.md`
2. **Индексные файлы Milvus** — см. ниже, самая дорогая по отдаче
3. **Полноценный бот в Telegram** — пользователи, команды, история, задачи;
   openclaw уходит за него маршрутизатором данных

---

## Индексные файлы Milvus раздуты в 22 раза

Замер 2026-08-16, бакет `a-bucket` (хранилище Milvus в MinIO), всего 64 ГиБ:

```
index_files    61 ГиБ    4227 объектов
insert_log    2.8 ГиБ    2918 объектов   ← собственно векторы
delta_log     1.9 МиБ     346 объектов
stats_log     1.2 МиБ     374 объекта
```

Индексы занимают в 22 раза больше, чем данные, которые они индексируют.

**Почему это важно.** Это и есть источник хронического давления на диск и роста
томов на ~3 ГБ в час во время ингеста. 14 августа заполнение дошло до 100%, Nebula
упала с `-3521`, стек пришлось поднимать вручную.

**Вероятная причина.** Milvus пишет новый набор индексных файлов при каждой
перестройке, старые должен убирать сборщик мусора. Похоже, он не работает или
сильно отстаёт.

**Гипотеза, которую стоит проверить первой.** Была миграция эмбеддингов
nomic → bge-m3 со сменой размерности на 1024 и полной очисткой. Если очистка не
дошла до MinIO, там до сих пор могут лежать индексы под 768 измерений — мёртвые,
но занимающие место.

**Чего делать нельзя.** Удалять файлы в `a-bucket` руками. Milvus считает их
своими; снос вручную ломает коллекцию. Путь — его собственные GC и компакция либо
контролируемая перестройка индекса.

Отдача ожидается в десятки гигабайт.

---

## Латентные ограничения

### Предел размера сообщения Temporal при большом пуле

Поиск передаёт весь объединённый пул чанков между шагами воркфлоу. При ~2255
байт на чанк пул около 1000 штук пересекает документированный предел 2 МиБ.

Достижимо настройками: `max_subqueries` до 20 (`src/config.py:633`), `top_k` до
100. По умолчанию 5 × 10 = 50, то есть в двадцать раз ниже.

Риск предсуществующий — `RerankParams` и раньше нёс весь пул на **вход**
активности, так что выход лишь стал симметричен входу. Но при подъёме этих
настроек поиск не деградирует, а падает с ошибкой размера. Знать до того, как
крутить ручки.

Связанное: за 256-м чанком (`_RERANK_SCORE_CAP`) ранжирование заканчивается, и
приписанный остаток несёт сырые скоры ретривера — список перестаёт быть
монотонным по `score`. Тот же режим «очень большой пул», та же цена.

---

## Технический долг, найденный по ходу

### `StatsSettings` — три ручки из четырёх мёртвые

`default_granularity`, `default_max_lag`, `search_limit` (`src/config.py`) не
читает никто, кроме теста. В коде на их местах литералы. При этом
`scripts/make_env.py` описывает все четыре как рабочие, с инвариантами
«>= 0» / «>= 1», которых ничто не проверяет. Оператор, выставивший
`STATS_SEARCH_LIMIT=50`, не получит ничего и не узнает об этом.

Решение бинарное: подключить к дефолтам инструментов или удалить вместе с
описаниями.

### Стаб БД в тестах не моделирует `row_factory`

`_StubConn.cursor()` (`tests/test_storage/test_stats_repository.py`) глотает
kwargs и всегда отдаёт словари. Следствие: удаление `row_factory=dict_row` из
`src/storage/stats.py` не уронит ни одного теста, хотя на живом psycopg3 чтения
станут кортежами и `r["score"]` упадёт. Целый класс типовых багов невидим.

### Перечисления granularity/value_kind продублированы

`VALUE_KINDS` / `GRANULARITIES` живут в `src/stats/align.py` и честно импортируются
всеми питоновскими поверхностями. Но CHECK-констрейнты в `scripts/setup_db.py` —
независимая копия, а тест пиннит эту копию как строку, а не сверяет с константой.
Из-за `CREATE TABLE IF NOT EXISTS` расширение перечисления в будущем **не доедет**
до существующей таблицы: потребуется ручной `ALTER TABLE ... DROP CONSTRAINT /
ADD CONSTRAINT`, чего ни код, ни документация не предусматривают. Четвёртая копия
granularity — `_TREND_GRANULARITIES` в `src/mcp/tools_server.py`.

### Нет теста на `except`-ветку оркестратора

Fail-open при падении активности реранка проверен только чтением кода. Внутренний
фолбэк самой активности («реранкер недоступен») тестом покрыт, а ветка
оркестратора — нет.

---

## Пред-существующие дефекты репозитория

### Дефолт графового бэкенда разошёлся с документацией и тестом

`src/config.py:154` задаёт `backend = "nebula"`, а докстринг на `:148` утверждает,
что дефолт остаётся `"neo4j"` до прохождения бенчмарков, и
`tests/test_config/test_graph_settings.py:5-9` тоже ждёт `"neo4j"`. Тест падает на
чистом чекауте, к нашим веткам отношения не имеет. Дефолт когда-то переключили,
комментарий и тест не поправили.

---

## Операционное

### ~~Перечитывает ли openclaw память без перезапуска~~ — закрыто

Проверено по времени: `AGENTS.md` изменён 2026-08-14 15:36, контейнер запущен
2026-08-15 19:31 — то есть он поднялся уже с новой памятью при выкате. Каталог
смонтирован живым бинд-маунтом. Отдельный перезапуск не нужен.

### Скрейпер источников статистики

Загрузка идёт через `POST /api/v1/statistics/load` вручную. Автоматический сбор —
отдельным процессом по образцу `tg_ingest`, не внутри ingest-воркера. Отложено до
подтверждения ценности сигнала.

### Векторный поиск по реестру показателей

Сейчас поиск показателя триграммный: находит опечатки и словоформы, не находит
синонимы («настроения» не найдёт «социальное самочувствие»). Escape-hatch —
отдельная Milvus-коллекция по образцу `entity_er_vec`. Заводить, когда упрёмся.
