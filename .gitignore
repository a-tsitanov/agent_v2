# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.pytest_cache/
.ruff_cache/

# Environments
.venv/
venv/
.env
.env.local
.env.*.local

# uv: the lockfile MUST be committed for reproducible builds
# (Dockerfile runs `uv sync --frozen`, which requires it).

# IDE

.idea/
*.swp

# Project data
/data/
*.log
.DS_Store

# Build
dist/
build/
research/

# Marp output
docs/presentation/*.pdf
docs/presentation/*.pptx
docs/presentation/*.html

# Git worktrees (non-hidden name so LlamaIndex SimpleDirectoryReader
# doesn't skip fixtures under it as "hidden").
worktrees/

.claude/settings.local.json

# Telethon session for the tg_ingest harness
*.session
*.session-journal
