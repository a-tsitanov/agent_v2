"""Synthetic scale-benchmark harness.

Zero production data: every graph / vector / entity set is generated
from a seed, so the whole suite runs without touching (or exporting
from) the production database.  See ``README.md`` for the rationale and
which cliff each module brackets.
"""
