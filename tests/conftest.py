"""Pytest-asyncio mode is set globally in pyproject.toml.

Project-wide fixtures land here as the codebase grows.  For Stage 0
the file exists primarily to mark the tests/ directory as a package
root for collection.
"""
